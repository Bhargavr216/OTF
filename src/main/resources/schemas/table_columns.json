{
  "job_queue_arch": [
    {
      "required": ["id", "orderid", "eventpayload", "poiresponse"],
      "optional": ["time", "jobsettime"],
      "ignore": []
    }
  ],
  "job_queue": [
    {
      "required": ["id", "orderid"],
      "optional": ["time", "jobsettime"],
      "ignore": []
    }
  ],
  "audit": [
    {
      "required": ["id", "orderid", "operation"],
      "optional": ["audit_id", "time", "jobsettime"],
      "ignore": []
    }
  ]
}
